package at.chaosfield.packupdate

import javafx.application.Application
import javafx.stage.Stage

object PackUpdate extends Application {
  override def start(stage: Stage): Unit = {
    stage.setTitle("Updating mods...")
  }
}
